{
  "name": "@nexus/shorts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The short-form repurposing engine: a finished long-form episode (video, transcript, narration timestamps, scene manifest, scene visual metadata) becomes structured vertical short candidates — scored on hook, curiosity, surprise, standalone clarity, payoff, emotion and visual strength, penalised for context dependence — and each candidate becomes a re-composed 9:16 plan (never a center crop).",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "clean": "tsc -b tsconfig.json --clean"
  },
  "dependencies": {
    "@nexus/audio": "workspace:*",
    "@nexus/scenes": "workspace:*",
    "zod": "^3.25.25"
  },
  "devDependencies": {
    "@nexus/characters": "workspace:*",
    "@nexus/db": "workspace:*",
    "@nexus/providers": "workspace:*",
    "@nexus/render": "workspace:*",
    "@nexus/storage": "workspace:*",
    "@nexus/video": "workspace:*"
  }
}
