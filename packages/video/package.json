{
  "name": "@nexus/video",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloud-compatible rendering pipeline: a validated scene manifest, the character library, the composed animation frames, the narration audio and the caption track become one real video file through a deterministic in-process rasteriser and FFmpeg \u2014 resumable by segment, reusable by content, with metadata, render logs and coded failure information.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "clean": "tsc -b tsconfig.json --clean"
  },
  "dependencies": {
    "@nexus/audio": "workspace:*",
    "@nexus/characters": "workspace:*",
    "@nexus/db": "workspace:*",
    "@nexus/jobs": "workspace:*",
    "@nexus/providers": "workspace:*",
    "@nexus/render": "workspace:*",
    "@nexus/scenes": "workspace:*",
    "@nexus/storage": "workspace:*",
    "zod": "^3.25.25"
  },
  "devDependencies": {
    "@nexus/config": "workspace:*"
  }
}
